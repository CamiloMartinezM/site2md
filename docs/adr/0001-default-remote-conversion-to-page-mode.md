# Default remote conversion to page mode

Remote URL conversion defaults to page mode, which fetches only the requested HTML document. The previous unlimited mirror behavior followed combinatorial navigation such as search filters while excluding semantically relevant links outside the URL path; explicit future follow and site modes can expand scope under their own selection rules and safety bounds.
